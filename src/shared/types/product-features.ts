export interface ProductFeatures {
  /**
   * Controls whether SkillsFan-hosted proxy AI models and credits UX are exposed.
   * When disabled, the desktop client only exposes user-owned API keys and
   * ChatGPT subscription login.
   */
  skillsfanHostedAiEnabled?: boolean
}
